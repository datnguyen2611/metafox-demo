import forum from './forum';
import forum_announcement from './forum_announcement';
import forum_post from './forum_post';
import forum_thread from './forum_thread';

const initialState = {
  forum,
  forum_thread,
  forum_post,
  forum_announcement
};

export default initialState;
