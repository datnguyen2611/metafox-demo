/**
 * @type: itemView
 * name: forum_search.itemView.mainCard
 * chunkName: forum
 */

import ItemView from './ItemView';

export { connectItemView } from '@metafox/framework';

export default ItemView;
