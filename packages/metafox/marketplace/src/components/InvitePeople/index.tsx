/**
 * @type: itemView
 * name: marketplace.itemView.peopleCard
 * chunkName: marketplace
 */
import { connectItemView } from '@metafox/framework';
import ItemView from './ItemView';

export default connectItemView(ItemView, () => {});
